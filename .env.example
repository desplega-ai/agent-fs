# agent-fs environment variables
# Copy to .env and fill in your values

# ── Core ──────────────────────────────────────────────
# Override default config directory (~/.agent-fs)
# AGENT_FS_HOME=~/.agent-fs

# ── Remote API (when connecting to a hosted agent-fs server) ──
# AGENT_FS_API_URL=https://your-server.example.com
# AGENT_FS_API_KEY=af_your_api_key_here

# ── S3 Storage (for remote S3 testing) ────────────────
# These override values in ~/.agent-fs/config.json
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_BUCKET=your-bucket-name
# S3_ACCESS_KEY_ID=AKIA...
# S3_SECRET_ACCESS_KEY=your-secret-key
# S3_REGION=us-east-1

# ── Embedding Providers (pick one) ────────────────────
# OpenAI (text-embedding-3-small, 768 dimensions)
# OPENAI_API_KEY=sk-...

# Google Gemini (gemini-embedding-001, 768 dimensions)
# GEMINI_API_KEY=AI...
