{
  "name": "agent-fs",
  "version": "0.1.1",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --build",
    "test": "bun test packages/*/src/",
    "test:coverage": "bun test --coverage packages/*/src/",
    "build": "./scripts/build.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "bun-types": "^1.2.0"
  }
}
